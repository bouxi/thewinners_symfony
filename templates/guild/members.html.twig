{% extends 'base.html.twig' %}

{% block title %}Membres de la guilde{% endblock %}

{% block body %}
    <div class="container py-4">

        <h1 class="mb-2">👥 Membres de la guilde</h1>
        <p class="text-muted mb-4">
            Découvrez les membres TheWinners (grade, avatar, classe et spécialisation).
        </p>

        {% if members is empty %}
            <div class="alert alert-secondary">
                Aucun membre pour le moment.
            </div>
        {% else %}
            <div class="row g-3">
                {% for m in members %}
                    {% set app = m.guildApplication ?? null %}

                    <div class="col-12 col-md-6 col-lg-4">
                        <div class="card bg-dark border-danger shadow-sm h-100">
                            <div class="card-body d-flex gap-3">

                                {# ✅ Avatar (fallback si null) #}
                                {% set avatarPath = m.avatar ? asset(m.avatar) : asset('images/default-avatar.png') %}

                                <img src="{{ avatarPath }}"
                                     alt="Avatar {{ m.pseudo }}"
                                     style="width:64px;height:64px;border-radius:12px;object-fit:cover;border:1px solid rgba(255,255,255,.2);">

                                <div class="flex-grow-1">

                                    <div class="d-flex align-items-center justify-content-between gap-2">
                                        <div class="fw-semibold text-white">
                                            {{ m.pseudo }}
                                        </div>

                                        {# ✅ Badge grade via enum label() #}
                                        <span class="badge bg-danger">
                                            {{ m.guildRank.label() }}
                                        </span>
                                    </div>

                                    <div class="mt-2 small text-light" style="opacity:.9;">
                                        <div>
                                            <span class="text-muted">Classe :</span>
                                            {{ app ? (app.class ?? '—') : '—' }}
                                        </div>

                                        <div>
                                            <span class="text-muted">Spé :</span>
                                            {{ app ? (app.specialization ?? '—') : '—' }}
                                        </div>
                                    </div>

                                </div>
                            </div>

                            {# Optionnel : footer #}
                            <div class="card-footer bg-transparent border-0 pt-0">
                                <div class="small text-muted">
                                    Membre #{{ m.id }}
                                </div>
                            </div>
                        </div>
                    </div>

                {% endfor %}
            </div>
        {% endif %}

    </div>
{% endblock %}
